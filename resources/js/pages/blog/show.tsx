import { BlogShowPage } from '@/features/blog/components/BlogShowPage';
import type { BlogShowPageProps } from '@/features/blog/types';

export default function BlogShow(props: BlogShowPageProps) {
    return <BlogShowPage {...props} />;
}
