import CategoryPage from '@/features/catalog/components/CategoryPage';
import type { CategoryPageProps } from '@/features/catalog/components/CategoryPage';

export default function Category(props: CategoryPageProps) {
    return <CategoryPage {...props} />;
}
