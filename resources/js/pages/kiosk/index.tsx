import KioskPage from '@/features/kiosk/components/KioskPage';
import type { KioskProps } from '@/features/kiosk/types';

export default function KioskIndex(props: KioskProps) {
    return <KioskPage {...props} />;
}
