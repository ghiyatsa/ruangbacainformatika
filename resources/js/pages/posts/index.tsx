import { PostIndexPage } from '@/features/posts/components/PostIndexPage';
import type { PostIndexPageProps } from '@/features/posts/types';

export default function PostIndex(props: PostIndexPageProps) {
    return <PostIndexPage {...props} />;
}
